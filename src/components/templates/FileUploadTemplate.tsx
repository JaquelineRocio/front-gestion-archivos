import React from "react";
import FileUploadForm from "../organisms/FileUploadForm";

const FileUploadTemplate: React.FC = () => (
  <div className="flex content-center h-svh justify-center">
    <FileUploadForm />
  </div>
);

export default FileUploadTemplate;
